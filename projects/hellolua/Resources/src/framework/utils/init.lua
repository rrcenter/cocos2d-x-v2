cc = cc or {}

cc.stringUtil = require(cc.PACKAGE_NAME .. '.utils.stringUtil')
cc.fixScreenUtil = require(cc.PACKAGE_NAME .. '.utils.fixScreenUtil')
cc.uiUtil = require(cc.PACKAGE_NAME .. '.utils.uiutils')